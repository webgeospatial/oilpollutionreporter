
//defining the data structure and type definitiion of what a locationClass is
//sat at this location so that the scope is global to the app
//rename this to something more useful
export class locationClassDefinition {
    id: number;
    state: string;
    lga: string;
    ward: string;
    x: number;
    y: number;
  }